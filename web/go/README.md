# Go web project

A minimal Go web server, created by the human Start Project wizard.

## Run

```sh
make run
```

Then open http://localhost:8080. Set `PORT` to use a different port.

## Test

```sh
make test
```
